config:
  plugins:
    expect: {}
  phases:
    - duration: 5
      arrivalRate: 100
  ensure:
    maxErrorRate: 1
scenarios:
  - name: 'test /unlimited'
    flow:
      - get:
          url: '/unlimited'
          expect:
            - statusCode: 204
