config:
  plugins:
    expect: {}
  phases:
    - duration: 5
      arrivalRate: 100
  ensure:
    maxErrorRate: 2
scenarios:
  - name: 'test /limited'
    flow:
      - get:
          url: '/limited'
          expect:
            - statusCode: 429
